sources = []

subdir('src')

lib_static = static_library('riscv-interpreter', sources, include_directories : 'include')
lib_shared = shared_library('riscv-interpreter', sources, include_directories : 'include')
